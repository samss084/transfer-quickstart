Shell:user/bin/bash
$ create  $//payload/app/com.onedebit.chim
e-0ahU91X1yPG1dPUh9BvLt
Q  returns ProcessPromise.
Everything passed through ${...} will be automatically escaped and quoted.

Run composer install ---ansi ---interaction --scripts ---progress --prefer-dist
  composer install ----ansi ---interaction ---scripts ---progress --prefer-dist
  shell: /usr/bin/bash 
let name = 'foo & bar'
await $`mkdir ${FOO}`
This folder is for our database that will be used to store users, accounts, and access tokens
